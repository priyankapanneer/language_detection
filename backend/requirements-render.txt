fastapi>=0.95,<1.0
uvicorn[standard]>=0.22,<0.23
gunicorn>=20.1.0
scikit-learn==1.3.2
joblib==1.5.2
pandas==2.1.3
numpy==1.25.3
scipy==1.11.2
python-dotenv==1.0.0

# Notes:
# - This file is a minimal, deployment-friendly subset of the project's
#   full requirements. It avoids heavy infra (pyspark, snowflake, datasets,
#   pyarrow) which commonly break installs on small cloud instances.
# - Target Python: 3.11 (see README_RENDER.md below). Pin versions only
#   where compatibility is important to avoid surprising build failures.